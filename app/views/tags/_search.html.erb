<% if user_signed_in? %>
<div class="search-form">
  <%= form_with url: tag_search_path, method: :get, local: true do |f| %>
    <%= f.text_field :tag %>
    <%= f.submit "タグ検索" %>
  <% end %>
</div>
<% end %>